{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run GLFW App",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/main.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "console": "externalTerminal",
      "MIMode": "gdb",
      "setupCommands": [
        { "text": "-enable-pretty-printing", "description": "Enable pretty printing", "ignoreFailures": true }
      ],
      "preLaunchTask": "Build GLFW App",
      "stopAtEntry": false
    }
  ]
}
